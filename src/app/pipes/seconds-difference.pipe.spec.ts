import { SecondsDifferencePipe } from './seconds-difference.pipe';

describe('SecondsDifferencePipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsDifferencePipe();
    expect(pipe).toBeTruthy();
  });
});
